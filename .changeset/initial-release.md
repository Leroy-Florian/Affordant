---
"@affordant/contract": minor
"affordant": minor
"@affordant/effect": minor
"@affordant/react": minor
"@affordant/server": minor
"@affordant/express": minor
"effect-react-bridge": minor
---

Initial public release of the Affordant package family: the shared wire contract (`@affordant/contract`), the vanilla Promise client (`affordant`), the Effect invoker (`@affordant/effect`), the React adapter (`@affordant/react`), the server-side envelope builder (`@affordant/server`), the Express adapter (`@affordant/express`), and the domain-agnostic Effect↔React bridge (`effect-react-bridge`).
