{
  "name": "affordant-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Affordance-first hypermedia (HATEOAS) toolkit: shared wire contract, vanilla client, server-side envelope builder, and framework adapters.",
  "license": "MIT",
  "author": "Florian Leroy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Leroy-Florian/Affordant.git"
  },
  "homepage": "https://leroy-florian.github.io/Affordant/",
  "bugs": "https://github.com/Leroy-Florian/Affordant/issues",
  "type": "module",
  "workspaces": [
    "packages/contract",
    "packages/client",
    "packages/server",
    "packages/express"
  ],
  "scripts": {
    "build": "npm run build -w @affordant/contract && npm run build -w affordant && npm run build -w @affordant/server && npm run build -w @affordant/express",
    "typecheck": "npm run build -w @affordant/contract && npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitepress": "^1.6.4",
    "vitest": "^4.1.8"
  }
}
